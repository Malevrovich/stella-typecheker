language core;
extend with #type-cast, #top-type, #structural-subtyping, #try-cast-as, #pairs;

fn dup(x : Top) -> { Top, Top } {
  return { x, x }
}

fn main(n : Nat) -> Nat {
  return try { dup(n) } cast as { Nat, Nat } { p => p.1 } with { 0 }
}
