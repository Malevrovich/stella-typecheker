INPUT:
language core;

extend with
  #unit-type,
  #references,
  #arithmetic-operators,
  #sequencing,
  #natural-literals;

fn main(n : Nat) -> fn(Nat) -> Nat {
  return foo(n)
}

fn foo(n : Nat) -> fn(Nat) -> Nat {
  return fn(n : Bool) {
    return n
  }
}

STDOUT:
(empty)

STDERR:
ERROR_UNEXPECTED_TYPE_FOR_PARAMETER:
Error occured at <unknown>:15:12:
n : Bool
Expected type Nat, but expr has type Bool

EXIT_CODE: 1
