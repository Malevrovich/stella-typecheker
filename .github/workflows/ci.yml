name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t stella-typechecker .

      - name: Run golden tests
        run: |
          docker run --rm \
            -v "${{ github.workspace }}/stella-examples:/workspace/stella-examples:ro" \
            -v "${{ github.workspace }}/tests:/workspace/tests:ro" \
            -w /workspace \
            --entrypoint python3 \
            stella-typechecker \
            tests/golden_tester.py \
              --binary build/typechecker/cli/typechecker \
              --stella-dir stella-examples \
              --golden-dir tests/golden
